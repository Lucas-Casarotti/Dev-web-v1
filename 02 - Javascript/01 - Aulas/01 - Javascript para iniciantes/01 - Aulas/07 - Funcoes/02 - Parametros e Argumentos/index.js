// Paramêtros -> É a variável que a função irá receber no escopo.
// Argumentos -> É o valor passado para a função.

// nome -> parâmetro
function ExibirNome (nome){
    console.log(`Olá, me chamo ${nome}`);
}

// nome -> argumento
var nome = 'Lucas'
ExibirNome(nome);